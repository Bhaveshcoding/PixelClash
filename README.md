# PIXEL CRASH

A 2D online multiplayer arena shooter game built with Python and pygame-ce

## How to Run
1. Activate your virtual environment.(.\.venv\Scripts\Activate.ps1)
2. Install dependencies: `pip install -r requirements.txt`
3. Run the game: `python src/main.py`